Задача. Извлеки все видимые данные только с одной страницы презентации (аренда/продажа коммерческой недвижимости). Верни ровно один JSON по схеме ниже.
Важно: ничего не нормализуй и не вычисляй. Сохраняй числа, валюты, единицы, даты, формат, регистр и переносы как в источнике. Блоки выводи в порядке чтения: сверху вниз, слева направо. Если чего-то нет — null или пустой массив. Повторы не удаляй.

Типы блоков: text, title, subtitle, table, cards, image, map, floorplan, contact, disclaimer, unknown.
Если элемент не подходит — используй unknown c полем text.
Выведи только JSON.
