{
  objects(карточки объектов/бизнес-центров)[] {
    object_rent_vat(влючен НДС на ставки в рамках объекта? / если упрощеннная система налогообложение (УСН) или упрощенка, то "не применяется" / если + НДС 5%, то "не применяется")
    object_name(название объекта/комплекса, если отсутствует то адрес объекта)
    sale_price_per_building(цена продажи всего здания)
    object_use_type(упоминается ли где то только один раз тип использования площадей: офис/ПСН/ритейл и т.д.)
    buildings(здания внутри объекта: название здания / номер строения: "стр. 1", "стр. 2" ... / null )[] {
      building_name(название/литера/строение/корпус)
      listings(помещения в этом здании)[] {
        use_type(тип использования или планировка: офис/ПСН/ритейл/студия и т.д.)
        area_sqm(значение всей площади)
        divisible_from_sqm(площадь делится от, м²; null)
        floor(значение этажа, если не число, то название эатажа)
        market_type(новый/ранее арендованные)
        fitout_condition(состояние: готово к въезду / готово к отделке / под отделку / с отделкой / с мебелью. Отображать только как "под отделку" или "с отделкой" )
        delivery_date(дата передачи: сейчас/дата/квартал-год, готовность к въезду, начало аренды. Отображать как "сейчас" или Дата)

        assignment_of_rights(переуступка прав: да/нет)
        assignment_details(высвобождение под клиента/замена арендатора, если указано)

        rent_rate(ставка аренды за м² в год)
        rent_cost_month_per_room(стоимость аренды всего помещения в месяц)
        rent_vat(в ставку аренды в год за м² включены НДС? / НДС базовой ставки / если упрощеннная система налогообложение (Упрощенка) или НДС 5%, то "не применяется")

        sale_price_per_sqm(цена продажи за м²)
        sale_vat(в цену продажи за м² включены НДС? / если упрощеннная система налогообложение (УСН) или упрощенка, то "не применяется" / если + НДС 5%, то "не применяется")

        opex_included(эксплуатационные расходы или плата за содержание здания или эксплуатацию и коммунальные платежи включены в ставку аренды в год за м² или оплачиваюся отдельно? )
        opex_year_per_sqm(эксплуатационные расходы в год за м² или плата за содержание здания )
      }
    }
  }
}
